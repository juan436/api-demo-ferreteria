# Server
PORT=3001
NODE_ENV=development

# MongoDB Local
MONGODB_URI=mongodb://localhost:27017/entidad

# JWT (solo desarrollo)
JWT_SECRET=dev_secret_change_me
JWT_EXPIRATION=7d

# CORS (frontend local)
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# Gmail SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=email@gmail.com
SMTP_PASS=pass

# Email settings
EMAIL_FROM="entidad <email@gmail.com>"